import { defineDesktopConfig } from '@owdproject/core/kit/authoring'

export default defineDesktopConfig({
  theme: '@owdproject/theme-nova',
  apps: ['@owdproject/app-fixture-valid'],
  modules: [],
})
